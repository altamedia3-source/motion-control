import "./globals.css";

export const metadata = {
  title: "Magnific Kling Motion Control",
  description: "Generate AI motion video with Magnific / Freepik API",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
